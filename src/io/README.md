# IO
- Legacy text input parsing for `.swp` cases and meteorological forcing (ttutil-based readers).
- Shared-simulation stubs and exchange hooks for coupled runs.
- Output writers for standard ASCII and CSV products.
