! Allocation-shape test for the 2D tables in heat_config_t (temtoptab,
! tembtab). The per-scalar default-zero and default-unallocated tests that
! once lived here were removed 2026-07-05: they only asserted compiler
! default-initialisation, not SWAP behaviour. Field parsing and validation
! are covered by test_read_heat_toml.pf and test_heat_config.pf.

@test
subroutine test_heat_config_table_2d_allocation()
   use funit
   use iso_fortran_env, only: real64
   use heat_config_mod, only: heat_config_t
   type(heat_config_t) :: cfg

   allocate(cfg%temtoptab(10, 2))
   allocate(cfg%tembtab(10, 2))
   cfg%temtoptab = 0.0_real64
   cfg%tembtab   = 0.0_real64

   @assertTrue(allocated(cfg%temtoptab))
   @assertTrue(allocated(cfg%tembtab))
   @assertEqual(10, size(cfg%temtoptab, 1))
   @assertEqual(2,  size(cfg%temtoptab, 2))
end subroutine
