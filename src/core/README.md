# Core
- Orchestration entry points (`swap`, `swap_main`) and state definitions.
- Initialization and time control utilities that configure global model state.
- Shared math/helpers (`functions`, `integral`, `checkmassbal`, grid conversion) and BMI wrapper.
