---
title: "RESUME HERE — T2-A exchange records / component-boundary arc"
date: 2026-07-04
status: active-handoff
---

# Resume point — 2026-07-04

Context was cleared mid-arc. This is the "start here" note. All work is on
branch **`development`**, ~30 commits **unpushed**. Everything below is committed
and byte-identical.

## The strategic arc (why we're doing this)

Redefining SWAP as a **vadose-zone engine with clean exchange interfaces** so
embedded models (WOFOST crop, ANIMO nutrients) become swappable *coupled
components* instead of forks compiled in. Governing ADRs:
- **ADR 0051** — SWAP-core vs coupled-component boundary (native = water/heat/
  solute + the table-driven "Plant"; components = detailed crop + nutrients).
- **ADR 0052** — nutrient subsystem **deleted** (~2.7k LoC, ~85 module globals;
  detailed nutrients → external ANIMO via the parked `.afo` output).
- **ADR 0053** — compartment **exchange records** (`state%exchange`), the
  mechanism that makes each coupling surface an explicit typed contract so a
  compartment's implementation can be swapped. Near-term goal: external WOFOST
  behind the `crop_water` record.

Design doc (field tables, protocol, deferrals):
`dev-docs/2026-07-03-arc-t2a-exchange-records-design.md`.
Roadmap: `dev-docs/2026-07-03-modernization-review.md` (§10 sprint map).

## What is DONE (all byte-identical: check-fast 4/4, pFUnit 806, check-bindings OK)

**Exchange-record machinery** (`src/state/exchange_state.f90`,
`swap_exchange_t` on `swap_state_t`) + the **four clean surfaces** routed through
`state%exchange`:
- `crop_water` — **soil-water coupling only**: root distribution (crop→sink,
  `RootExtraction`) + transpiration feedback (water→crop, `reltr`).
- `heat_soil`, `drain_soil`, `atmos_soil` — fully routed.

**The pattern** (reuse it): (1) add the record type + a `swap_exchange_t` field;
(2) POPULATE it in the orchestrator (`src/driver/swap_mod.f90`) with a pure copy
at the correct cadence point (after producer, before consumer); (3) add
`exch => state%exchange` to the consumer's `associate` and repoint its
reach-across reads. Gate: `pixi run clean && pixi run -e test check-fast` (4/4
byte-identical, pFUnit 806) **and** `pixi run -e test check-bindings` (all OK).

## DEFERRED (the remaining work, in priority order)

1. ~~**Push + watch CI first**~~ **DONE 2026-07-04** — pushed through `0947d42`;
   CI `test` green. Two CI-only fixes were needed on the way (neither touches
   physics): (a) `ci:` bump pinned pixi `v0.47.0 → v0.70.2` because the committed
   `pixi.lock` is format v7 (written by local pixi 0.70.2) and the old pin only
   read up to v6; (b) `ci:` made `subprojects/test-drive.wrap` a self-contained
   `wrap-git` instead of a redirect into `toml-f/subprojects/...` — meson 1.7.0
   (pulled by the newer pixi) eagerly validates redirect targets at `meson setup`
   before toml-f is fetched, and toml-f/ is now untracked, so fresh checkouts
   failed. Both verified locally (fresh-checkout `meson setup` OK; check-fast 4/4).
2. ~~**`crop_water` atmosphere-ET sub-part.**~~ **DONE 2026-07-04** — two
   byte-identical commits: (a) relocated `et0/es0/ew0` off crop state onto
   `state%atmosphere` (ET-step intermediates, never read by crop — the untangle);
   (b) routed the day's canopy state (`lai`, `crop_height`=ch, `interc_demand`=gird,
   `co2_transp_fac`=fco2tra) crop→atmosphere through `state%exchange%crop_water`,
   populated after `CropGrowth(1)`+irrigation and before `ProcessMeteoDay`.
   Remaining direct atmosphere→crop reads: config-constants (kdif/kdir/cf/… stay
   config-passed) + still-direct dynamic crossings (`gc`/`flCropEmergence`/
   phenology flags) — follow-up, not blocking the WOFOST plug.
3. **`solute` surface** — a many-to-one CONSUMER (reads water flux + temperature
   + drainage flux + irrigation/rain conc from 4 compartments) → a
   "transport-drivers" bundle record, not a bilateral pair.
4. **`surfacewater` surface** — `pond`/`runots`/`hsurf` bidirectional with the top
   boundary; needs its own protocol design.
5. **THE PAYOFF (bigger, later):** externalize WOFOST behind `crop_water` over
   BMI/XMI — same shape as the working SWAP↔MODFLOW 6 coupling. First needs the
   crop↔atmo ET sub-part (2) so the full crop interface is a contract.

## Non-negotiables (don't forget)

- **Byte-identical regression is the law** (`swap420gf` oracle; `check-fast`).
- Copy semantics in the records (values, not pointers) — external-ready.
- Clean rebuild after any `src/state/*.f90` change (`.mod` cross-boundary).
- Commit only when asked; never push `origin/main`; work on `development`.
- The six-path binding gate is `pixi run -e test check-bindings`.

## Recommended next step

**Push the 30 commits and confirm the CI test run is green**, then resume with
deferred item **2 (crop↔atmosphere ET untangling)** — it completes the crop
interface, which is the prerequisite for the WOFOST externalization payoff.
</content>
